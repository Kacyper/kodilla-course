package com.kodilla.testing.shape;

public class ShapeCollector {
    public Shape getField(int n) {
        if (n < shapeList.size() && n > 0) {
            if (n < shapeList.size() && n >= 0) {
                return shapeList.get(n);
            } else {
                return null;
}
